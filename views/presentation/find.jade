extends ../layout
block body
  .container
    h1= presentation.title
    h3= presentation.level
    p= presentation.abstract
    if(req.user.id == presentation.userId || req.user.isAdmin)
      a.btn.btn-primary(href='/presentation/update/' + presentation.id) Edit

